fm_elfinder:
    instances:
        default:
            editor: "form"
            include_assets: true
            relative_path: true
            connector:
                roots:
                    uploads:
                        driver: 'LocalFileSystem'
                        path: 'uploads'
                        upload_allow: "all"
