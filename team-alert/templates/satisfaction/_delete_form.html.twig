<form method="post" action="{{ path('satisfaction_delete', {'id': satisfaction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ satisfaction.id) }}">
    <button class="btn">Delete</button>
</form>
